# ХЕНДОФФ: дилерская EMAIL-кампания — ЧТО отправлять и КАК работать с базой

> Дата: 2026-07-15. Передача в новый чат. Фокус нового чата: **контент писем + механика рассылки и
> взаимодействия с базой дилеров**. Сбор базы и инфраструктура — УЖЕ СДЕЛАНЫ (см. ниже).

---

## 1. КОНТЕКСТ (зачем это всё)

Бизнес owner (**АлюмКомплект**, `alumkomplekt.bitrix24.ru`) — B2B поставка алюминиевых
светопрозрачных конструкций. Кассовый кризис. Диагноз по Bitrix (прошлый чат):
- Проблема **НЕ в лидах** (3-5 КП/день) и **НЕ в цене** (−30% рынка, 90% хвалят цену).
- Проблема в **закрытии/сегменте**: 330М ₽ висит в «Подготовка КП», выиграно 6 сделок = 2.9М ₽,
  win 12%. Тендерные подрядчики «ждут денег заказчика» → не платят.
- **Кто реально платит:** розничные дилеры/монтажники (повторные, из НЕ-тендерных каналов).

**Разворот → дилерский EMAIL-канал.** Идеальный клиент (ICP, owner):
> **ИП, офис+сайт, крупный город. Продаёт В РОЗНИЦУ частнику (остекление дома/веранды/беседки/
> балкона) СО СВОИМ монтажом, но БЕЗ своего производства** (→ обязан покупать готовые конструкции =
> идеальный дилер). Клиентов берёт из интернета. Тендеров мало/нет.
Почему сработает: у него розничный кэш (частник платит сразу, без «ждём денег»), нет цеха →
покупает у нас, наши −30% идут в его маржу, берёт регулярно.
Канал — **EMAIL** (owner: лучшие заказы приходили почтой; телефон многих напрягает).

---

## 2. ЧТО УЖЕ ГОТОВО (не переделывать)

### База дилеров — собрана
- **`reports/DEALERS_RETAIL.csv`** — 553 email, из них **342 «розница✓»** (целевые), 201 «?»
  (на ручную проверку), 10 «производитель✗» (исключить), 42 с флагом **ИП**.
- Колонки: `email, company, site, fit, ИП, retail_score, prod_score`.
  ⚠️ Города/ФИО/телефона в CSV НЕТ — персонализация пока только по `company` + `site` + `fit`.
- Как собрано: 397 авто-запросов Serper → 753 домена → скрап почты + классификатор
  (`tb_retail_pool.py`) розница/производитель/ИП. Дедуп против Bitrix уже сделан (никого из базы
  Bitrix в листе нет).
- Все, кто уже в Bitrix (клиенты/лиды) — исключены на этапе сбора (`tb_bitrix_clients.client_keys()`).

### Инфраструктура — готова и рабочая
- **Unisender GO ПОДТВЕРЖДЁН и ЖИВОЙ:** домен `alumkomplekt-sale.ru` confirmed, DKIM active,
  отправитель `info@alumkomplekt-sale.ru`. Проверка: `python tb_unisender.py` (покажет статусы).
- **Отправка:** `tb_unisender.py` → `send(to_addr, subject, body, attachments=None)`.
  Сам добавляет ссылку отписки (анти-спам), track_read/track_links включены.
- **Bitrix:** `tb_bitrix.py` → `create_lead(title, company, phone, email, comments, source_id)`.
- **Входящие/ответы:** `tb_mail.py`, `tb_triage.py` (IMAP ящика менеджера, креды `MANAGER_IMAP_*` в .env).
- **Существующий кампанийный движок (от тендерной рассылки, можно адаптировать):**
  `tb_outreach.py`, `tb_campaign_load.py`, `tb_pool.py`, `tb_main.py`, `tb_bot.py`, `tb_netguard.py`.

### Контент/стратегия — черновики готовы
- **`reports/DEALER_OFFER_AND_SCRIPT.md`** — ICP, оффер (4 крючка: −30% в маржу, срок неделя,
  supply-only=не лезем к их клиенту, тест-заказ), письмо №1, скрипт звонка (вторично).
- **`reports/CONVERSION_PLAYBOOK.md`** — 7 рычагов отклика + **серия из 3 писем** (с темами A/B) +
  деливерабильность + план измерения/итерации.
- **`reports/DEALER_PRICELIST_TEMPLATE.md`** — шаблон прайса на типовые позиции.

---

## 3. ФОКУС НОВОГО ЧАТА: ЧТО отправлять + КАК работать с базой

### A. ЧТО отправлять (контент) — доработать с owner
1. **Финализировать письмо №1** (взять из `CONVERSION_PLAYBOOK.md` §Письмо 1, адаптировать).
   Главный рычаг — **конкретная цифра-якорь** на типовую позицию (входная группа 2100×2100 /
   остекление балкона за м² и т.п.).
   🔴 **ЗАВИСИМОСТЬ:** движок пока НЕ считает объекты. Цифра берётся из **стандартного прайса**
   (owner знает цены; отдельный агент делает прайс для сайта — уточнить, готов ли).
2. **Финализировать серию:** письмо №2 (снятие риска, через 3-4 дня не-ответившим), письмо №3
   («закрывать ваш вопрос?» — даёт самый высокий отклик, через 5-6 дней).
3. **Что слать ответившему:** дилерский прайс (файл/ссылка). Зависит от прайса выше.
4. **A/B темы** письма №1 (2 варианта в playbook) — на первой партии.

### B. КАК работать с базой (механика) — спроектировать/собрать
1. **Кого шлём первыми:** 342 «розница✓». 201 «?» — после беглой ручной проверки (или отдельной партией).
2. **Кампанийный раннер** (новый скрипт ИЛИ адаптация `tb_outreach.py`+`tb_campaign_load.py`):
   грузит `DEALERS_RETAIL.csv` → фильтр `fit==розница✓` → персонализация `{company}` →
   шлёт партиями **25-30/день с прогревом** (не 342 разом — новый домен сожжётся) → лог отправленных
   (чтобы не слать дважды) → расписание касаний 2 и 3.
3. **Обработка ответов:** IMAP (`tb_mail`/`tb_triage`) → ответивший автоматом в Bitrix
   `create_lead(source_id="DEALER_OUTREACH")` + уведомление owner (Telegram есть: `tb_telegram.py`).
4. **Метрики:** Open% (Unisender track_read), Reply% (ящик), в отдельный лог/отчёт; недельная итерация.
5. **Гигиена:** отписки (Unisender сам), bounce-обработка, не-слать повторно, стоп-лист.

### C. Открытые решения для owner (спросить в начале нового чата)
- Готов ли **прайс на типовые позиции** (цифра-якорь для письма)? Кто/когда даёт числа?
- Слать 342 «розница✓» сразу или сначала ручная проверка 201 «?»?
- Размер партии/день, часы отправки, с какого адреса подпись (имя менеджера, WhatsApp/тел)?
- Ответы вести вручную или авто-лид в Bitrix + Telegram-пинг?
- Вложение прайса в письмо №1 или ссылкой/во втором касании (вложение = риск спама)?

---

## 4. ГОТЧИ / ГРАНИЦЫ (важно не сломать)

- **Прогрев обязателен.** Новый домен: старт 20-30 писем/день, наращивать. Иначе спам-бан домена.
- **Первое письмо — без вложений**, текст «человеческий», реальная подпись, заканчивать вопросом.
- **Движок НЕ считает объекты** — не обещать авто-расчёт/«цену за час»; честно «посчитаю за 1-2 дня»,
  цифра-якорь только из стандартного прайса.
- **Не трогать** старых дилеров (owner общается сам) и мёртвый тендерный пайплайн.
- **B2B supply-only** — не добавлять монтаж/розницу-конечнику в оффер (мы поставщик для дилера).
- В CSV нет города/ФИО — если нужна глубокая персонализация, город можно ре-извлечь с сайта/из
  запроса Serper (или добавить колонку при следующем скрапе).
- Ключи в `.env`: `UNISENDER_GO_API_KEY`, `CAMPAIGN_FROM_EMAIL`, `SERPER_API_KEY`, `BITRIX_WEBHOOK`,
  `MANAGER_IMAP_*`, `TELEGRAM_*`. Не коммитить.

---

## 5. ПЕРВЫЕ ШАГИ В НОВОМ ЧАТЕ
1. `python tb_unisender.py` — убедиться, что домен confirmed/DKIM active (сейчас — да).
2. Спросить owner про прайс-цифру и решения из §3.C.
3. Финализировать письмо №1 + серию (из playbook).
4. Собрать/адаптировать кампанийный раннер (партии+прогрев+лог+касания 2/3+ответы→Bitrix).
5. Тест: отправить 1 письмо на свой адрес → проверить вид/спам/отписку → запустить первую партию 25-30.
